# Toy Cycle Brief

This is the compact context for the toy sleep experiment.

## Objective

Run three independent toy tasks:

- `toy-a`
- `toy-b`
- `toy-c`

Each task sleeps for a few seconds and writes a JSON result.

## Expected Result

Each output directory should contain:

- `completed.json`

The result is considered complete when all three files exist.

